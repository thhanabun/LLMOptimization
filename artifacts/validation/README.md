# Validation Artifacts

This directory is the parking area for generated benchmark and certification outputs when a run needs to be preserved for review.

Generated `*.json`, `*.csv`, and `*.html` outputs are ignored by default. Keep root clean for release work, and commit only curated summaries or docs that explain the evidence.
